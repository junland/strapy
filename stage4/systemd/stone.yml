name        : systemd
version     : 249.4
release     : 1
summary     : A System and Service Manager
license     :
    - GPL-2.0-or-later
    - LGPL-2.1-or-later
    - MIT
homepage    : http://www.freedesktop.org/wiki/Software/systemd
description : |
    A System and Service Manager
upstreams   :
    - https://github.com/systemd/systemd-stable/archive/v249.4.tar.gz : f3df809bbe777fcb5ffe2b29aaf6f871de764ef22706940960f88b8a850e9278
setup       : |
    %meson \
        -Defi=true \
        -Dfuzz-tests=false \
        -Dinstall-tests=false \
        -Dslow-tests=false \
        -Dsplit-bin=false \
        -Dsplit-usr=false \
        -Dtests=false \
        -Dzstd=true
build       : |
    %meson_build
install     : |
    %meson_install

    # Stateless: Clean up /var directories, they are created via tmpfiles already
    rm %(installroot)/var/log/README
    rmdir %(installroot)/var/{lib/systemd,lib,log/journal,log,}

    # Stateless: Clean up /etc, contains mostly example configs to learn from that we should put somewhere
    # Actual vendor files can go in /usr/lib/systemd/journald.conf.d for example
    # Test via systemd-analyze cat-config systemd/journald.conf
    rm -rf %(installroot)/etc
